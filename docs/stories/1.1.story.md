# Story 1.1: Fondation applicative et point de santé

## Status

Done

## Story

**En tant qu'** équipe de développement,
**je veux** une application Laravel initialisée et déployable,
**afin que** toute story ultérieure s'appuie sur une base testée.

## Acceptance Criteria

1. Application Laravel 13 / PHP 8.3 en structure « slim » (middleware, exceptions et routes dans `bootstrap/app.php`), démarrable par `php artisan serve` sans erreur.
2. Inertia.js 2 + Vue 3 + Vite 8 + Tailwind 4 en configuration CSS-first sont câblés ; une page de démonstration se rend.
3. Les cinq espaces de noms de modules existent sous `Http/Controllers`, `Models`, `Policies`, `Services` et `resources/js/Pages` : `Platform`, `Identity`, `Work`, `Accountability`, `Finance`. Aucun dossier racine nouveau n'est créé.
4. `/up` retourne en HTTP 200 : version applicative, état de la connexion base, état du cache, espace disque libre, horodatage en `Africa/Niamey`. Le point est accessible **sans authentification** et n'expose aucun secret, chemin sensible ni nom d'hôte interne. La route `health: '/up'` par défaut de `bootstrap/app.php` est **remplacée** — elle ne retourne qu'une page vide.
5. `/up` retourne un statut d'échec explicite lorsque **la base** est injoignable ; testé en coupant la connexion. Chaque composant porte son **état individuel** : un cache indisponible dégrade la réponse sans la faire échouer. En local, Redis peut être absent — `php artisan serve` et `/up` doivent fonctionner sans lui.
6. `php artisan test` passe intégralement ; `vendor/bin/pint --dirty` ne remonte aucune violation.
7. Le dépôt git est initialisé avec une branche `main` et un **premier commit** contenant l'application, `.bmad-core/`, `AGENTS.md` et `docs/`. Sans cela, la protection de branche de la story 1.3 n'a rien à protéger, et Codex Web ne voit ni les agents ni le backlog.
8. `.gitignore` exclut `vendor/`, `node_modules/`, `.env`, `storage/app/private/`, `database/database.sqlite` et `.ai/`. ⛔ Un test de la chaîne échoue si un fichier `.env` est versionné.
9. Un `README.md` décrit l'installation locale de bout en bout, vérifiée sur une machine vierge : `export PATH` vers PHP 8.3 de MAMP, alias Composer, `composer install`, `npm ci`, copie de `.env.example`, `php artisan key:generate`, `php artisan migrate`, `npm run dev`, `php artisan serve`.
10. `.env.example` est complet et à jour : base, Redis, fuseau `Africa/Niamey`, locale `fr`, aucun secret réel.

### Décisions de migration et d'environnement propres à la story

Cette story ne crée aucune table métier et traite explicitement les migrations livrées par Laravel :

| Migration Laravel par défaut | Décision en 1.1 | Motif |
| --- | --- | --- |
| `create_users_table` (`users`, `password_reset_tokens`, `sessions`) | **Supprimée** | Ces tables seront recréées en 2.1 selon le modèle personne/compte ; les conserver contournerait l'ordre `audit_logs` d'abord. |
| `create_cache_table` | **Supprimée** | Le cache cible est Redis ; le cache local de 1.1 utilise le pilote fichier. |
| `create_jobs_table` (`jobs`, `job_batches`, `failed_jobs`) | **Conservée** | Infrastructure sans donnée métier ; `failed_jobs` sera utilisée par la supervision. |

Un test bloquant exécute `migrate:fresh` et vérifie qu'aucune table `users` n'existe.

En 1.1, `SESSION_DRIVER=file`, `CACHE_STORE=file` et `QUEUE_CONNECTION=sync` permettent le démarrage local sans Redis. Les sessions passent en base en 2.4 ; Redis devient le cache et la file de préproduction et de production en 1.5. L'audit est sans objet dans cette story : `audit_logs` est créée en 1.4.

## Tasks / Subtasks

- [x] **Task 1 — Stabiliser le squelette Laravel et l'état initial du dépôt** (AC : 1, 7)
  - [x] Vérifier PHP 8.3 et Laravel 13 avec la structure slim déclarée dans `bootstrap/app.php`.
  - [x] Vérifier le démarrage par `php artisan serve` avec le PHP 8.3 de MAMP.
  - [x] Confirmer que `main` et le premier commit contiennent l'application, `.bmad-core/`, `AGENTS.md` et `docs/`, sans réécrire l'historique existant.
  - [x] Supprimer les migrations Laravel par défaut `create_users_table` et `create_cache_table`, conserver `create_jobs_table`, puis vérifier qu'aucune table métier n'est créée. [Source: architecture/database-schema.md#ordre-de-dépendance-des-migrations]
- [x] **Task 2 — Câbler le socle frontend** (AC : 2)
  - [x] Installer et configurer Inertia.js 2, Vue 3, Vite 8 et Tailwind 4 en mode CSS-first.
  - [x] Configurer l'amorçage Inertia dans `resources/js/app.js`.
  - [x] Créer une page Vue de démonstration dans `resources/js/Pages/Platform/` et la rendre depuis une route Laravel nommée.
  - [x] N'utiliser ni SSR, ni SPA séparée, ni bibliothèque de composants, ni ressource tierce à l'exécution. [Source: architecture/tech-stack.md#interdits] [Source: architecture/frontend-architecture.md#contraintes-qui-pèsent-sur-chaque-page]
- [x] **Task 3 — Créer les cinq structures modulaires** (AC : 3)
  - [x] Créer `Platform`, `Identity`, `Work`, `Accountability` et `Finance` sous `app/Http/Controllers/`.
  - [x] Créer les mêmes sous-dossiers sous `app/Models/`, `app/Policies/`, `app/Services/` et `resources/js/Pages/`.
  - [x] Ne créer aucun dossier racine ni paquet Composer supplémentaire. [Source: architecture/source-tree.md#arborescence] [Source: architecture/source-tree.md#où-placer-quoi]
- [x] **Task 4 — Configurer le socle d'environnement local** (AC : 5, 10)
  - [x] Configurer `SESSION_DRIVER=file`, `CACHE_STORE=file` et `QUEUE_CONNECTION=sync` pour que l'installation locale fonctionne sans Redis.
  - [x] Garder les horodatages applicatifs en UTC et configurer leur affichage en `Africa/Niamey`. [Source: architecture/17-fuseau-horaire-et-devise.md#171-temps-nfr23--dec-01]
  - [x] Mettre `.env.example` à jour pour la base, Redis, la locale française, le fuseau d'affichage et les pilotes locaux, sans secret réel.
  - [x] Documenter la transition prévue vers Redis en préproduction/production et vers les sessions en base à partir des stories concernées, sans l'anticiper dans cette story. [Source: architecture/24-environnements.md#241-les-quatre-environnements]
- [x] **Task 5 — Livrer le point de santé public `/up`** (AC : 4, 5)
  - [x] Remplacer la route `health: '/up'` minimale actuellement déclarée dans `bootstrap/app.php`.
  - [x] Retourner la version applicative, l'état individuel de la base et du cache, l'espace disque libre et un horodatage affiché en `Africa/Niamey`.
  - [x] Garantir l'accès sans authentification et ne jamais exposer de secret, d'identifiant de connexion, de chemin sensible ou de nom d'hôte interne.
  - [x] Retourner un échec HTTP explicite lorsque la base est indisponible.
  - [x] Dégrader uniquement l'état individuel du cache lorsque celui-ci est indisponible, sans faire échouer la réponse globale ni le démarrage local.
  - [x] Ne pas exposer l'âge de la dernière sauvegarde, réservé à la story 11.1. [Source: architecture/22-journaux-erreurs-et-observabilit.md#224-observabilité]
- [x] **Task 6 — Sécuriser et documenter l'installation locale** (AC : 8, 9, 10)
  - [x] Ajouter à `.gitignore` toutes les exclusions exigées, notamment `.ai/` et `database/database.sqlite`.
  - [x] Ajouter un contrôle automatisé échouant si un fichier `.env` est suivi par Git.
  - [x] Remplacer le README Laravel générique par une installation complète en français.
  - [x] Documenter `export PATH="/Applications/MAMP/bin/php/php8.3.30/bin:$PATH"`, `alias composer='php /Applications/MAMP/bin/php/composer'`, puis toutes les commandes d'installation exigées.
  - [x] Vérifier et consigner la procédure sur une installation propre.
- [x] **Task 7 — Couvrir les critères et exécuter les validations** (AC : 1–10)
  - [x] Ajouter au moins un test Feature par critère d'acceptation applicable.
  - [x] Tester le démarrage du socle et le rendu de la page Inertia de démonstration.
  - [x] Tester le contenu public et expurgé de `/up`, ainsi que l'état individuel de chaque composant.
  - [x] Tester `/up` avec une base indisponible, puis avec un cache indisponible, et vérifier les deux comportements distincts.
  - [x] Exécuter `migrate:fresh` dans un test et vérifier explicitement que la table `users` n'existe pas.
  - [x] Tester les règles de propreté du dépôt et l'absence de fichier `.env` suivi par Git.
  - [x] Exécuter `php artisan test`, `vendor/bin/pint --dirty`, Larastan niveau 6 et `npm run build`. [Source: architecture/testing-strategy.md#pyramide] [Source: architecture/coding-standards.md#avant-de-finaliser]

## Dev Notes

### Previous Story Insights

Aucune story précédente. La story 1.1 est le premier incrément du plan d'exécution canonique en 11 epics et bloque toutes les stories suivantes. [Source: architecture/28-suites-donner.md#ordre-dimplémentation-imposé-par-larchitecture]

### Socle transverse — applicable sans être répété

Les onze règles **SOC-01 à SOC-11** de `docs/prd/socle-transverse.md` font partie de la définition de
terminé de cette story comme de toutes les autres. Elles ne sont pas recopiées dans les critères
d'acceptation ; le fichier est chargé automatiquement à chaque story via `devLoadAlwaysFiles`.

Celles qui mordent concrètement ici : **SOC-04** (une migration déployée n'est jamais modifiée),
**SOC-05** (un test par critère, Pint propre, Larastan 6), **SOC-09** (320 px, 3 s en 3G, aucune
ressource tierce) et **SOC-11** (types de retour explicites, `env()` seulement dans `config/`,
horodatages UTC affichés à Niamey). [Source: prd/socle-transverse.md]

### Architecture et stack

- Monolithe modulaire Laravel rendu avec Vue par Inertia, dans un artefact unique. Pas d'API publique, de frontend séparé, de SSR, de PWA ou de microservice. [Source: architecture/3-vue-densemble.md#31-style-architectural] [Source: architecture/3-vue-densemble.md#32-ce-que-larchitecture-ne-fait-pas]
- Stack imposée : PHP 8.3, Laravel 13, Inertia 2, Vue 3.5+, Vite 8 et Tailwind 4 CSS-first. PHPUnit 12 est utilisé, jamais Pest. [Source: architecture/tech-stack.md#tech-stack]
- Middleware, exceptions et routes sont configurés dans `bootstrap/app.php`. [Source: architecture/tech-stack.md#points-dattention-laravel-13]

### Données et migrations

- Cette story ne crée aucun modèle ni aucune table métier.
- `audit_logs` doit précéder toute table métier ; une migration déployée ne doit jamais être modifiée. [Source: architecture/database-schema.md#ordre-de-dépendance-des-migrations] [Source: architecture/database-schema.md#règles-non-négociables]
- La migration d'infrastructure des files peut subsister, mais les migrations Laravel créant prématurément des données métier doivent être retirées conformément aux exigences de l'epic.
- Les horodatages sont stockés en UTC et affichés en `Africa/Niamey`. L'horodatage exposé par `/up` doit donc être formaté pour Niamey sans modifier le fuseau de stockage. [Source: architecture/17-fuseau-horaire-et-devise.md#171-temps-nfr23--dec-01]

### Point de santé et contrats HTTP

- `/up` constitue l'exception JSON d'exploitation au modèle Inertia ; il ne crée pas d'API publique. [Source: architecture/coding-standards.md#laravel] [Source: architecture/rest-api-spec.md#rest-api-spec]
- Le point de santé couvre la base, le cache et le disque. L'âge de la sauvegarde sera ajouté uniquement en story 11.1. [Source: architecture/22-journaux-erreurs-et-observabilit.md#224-observabilité]
- Le contrat public, les états individuels et le comportement dégradé du cache sont définis par les critères d'acceptation de cette story.
- Aucune intégration externe n'est nécessaire. [Source: architecture/external-apis.md#external-apis]

### Frontend

- `resources/js/app.js` porte l'amorçage Inertia ; les pages sont placées sous `resources/js/Pages/{Module}/`. [Source: architecture/source-tree.md#arborescence]
- La page de démonstration reste minimale : le système de design transverse appartient à la story 1.7.
- Aucune bibliothèque de composants ni ressource tierce ne doit être chargée à l'exécution.
- La page doit respecter le budget initial de 300 Ko, fonctionner à 320 px et ne porter aucune information uniquement par la couleur. [Source: architecture/frontend-architecture.md#contraintes-qui-pèsent-sur-chaque-page]

### File Locations

- `bootstrap/app.php` — configuration slim et remplacement du point `/up` par défaut.
- `routes/web.php` — route nommée de démonstration et route publique de santé si nécessaire.
- `app/Http/Controllers/Platform/` — contrôleur de santé si cette responsabilité est extraite.
- `app/{Models,Policies,Services}/{Module}/` — structures des cinq modules.
- `resources/js/app.js` — amorçage Inertia.
- `resources/js/Pages/Platform/` — page de démonstration.
- `resources/css/app.css` — Tailwind 4 CSS-first.
- `database/migrations/` — retrait des migrations Laravel incompatibles et conservation de l'infrastructure des jobs.
- `tests/Feature/` et `tests/Feature/Http/` — tests du socle, de `/up` et des migrations.
- `.env.example`, `.gitignore`, `README.md` — configuration et installation locale. [Source: architecture/source-tree.md#arborescence] [Source: architecture/source-tree.md#où-placer-quoi]

### Testing

- PHPUnit uniquement.
- Tests Feature pour le démarrage, le rendu Inertia, le contrat `/up`, ses chemins dégradés, l'état des migrations et l'hygiène du dépôt.
- Le test de migration doit exécuter `migrate:fresh`, puis vérifier explicitement que la table `users` n'existe pas.
- Les chemins nominaux, d'erreur et les cas limites doivent être couverts.
- Les commandes finales sont `php artisan test`, `vendor/bin/pint --dirty`, Larastan niveau 6 et `npm run build`. [Source: architecture/testing-strategy.md#pyramide] [Source: architecture/testing-strategy.md#règles-générales] [Source: architecture/coding-standards.md#avant-de-finaliser]

### Project Structure Notes

Le dépôt suit déjà la racine Laravel standard et contient Laravel 13.8, PHP 8.3, Vite 8, Tailwind 4, `main` et un premier commit. Les écarts observés à traiter sont :

- Inertia et Vue ne sont pas encore câblés ;
- `resources/js/app.js` est vide ;
- les sous-dossiers des cinq modules n'existent pas ;
- le `/up` Laravel par défaut est déclaré mais ne fournit pas le contrat exigé ;
- les migrations Laravel par défaut créent prématurément `users` ;
- `.env.example` est encore en anglais et utilise des pilotes incompatibles avec l'étape 1.1 ;
- `.gitignore` n'exclut pas encore `.ai/` ni `database/database.sqlite` ;
- le README est encore le document générique Laravel.

Aucun nouveau dossier racine n'est nécessaire. [Source: architecture/source-tree.md#arborescence]

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-07-18 | 1.0 | Création de la story 1.1 et intégration des décisions sur les migrations et le point de santé. | Bob (Scrum Master) |
| 2026-07-18 | 1.1 | Implémentation complète du socle, tests et corrections demandées par la QA. | James (Developer) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Manifest Vite absent pendant les premiers tests HTTP : Vite neutralisé dans `tests/TestCase.php`.
- Larastan absent des dépendances directes : ajout de `larastan/larastan` et configuration niveau 6.
- Suivi QA : suppression du squelette `User`, réalignement SQLite par `migrate:fresh` et assertion d'horodatage découplée de l'ordre JSON.

### Completion Notes List

- Laravel 13/PHP 8.3 et le démarrage réel par `artisan serve` ont été vérifiés.
- Inertia 2, Vue 3, Vite 8 et Tailwind 4 rendent la page `Platform/Demo` sans ressource tierce à l'exécution.
- `/up` expose uniquement version, états base/cache/disque et horodatage Niamey ; base indisponible = 503, cache indisponible = 200 dégradé.
- La base SQLite locale a été réalignée : seules les tables d'infrastructure des jobs sont créées.
- Validation finale : 13 tests, 117 assertions, Pint propre, Larastan niveau 6 sans erreur et build Vite réussi.

### File List

- `.env.example`
- `.gitignore`
- `README.md`
- `app/Http/Controllers/Platform/HealthController.php`
- `app/Http/Middleware/HandleInertiaRequests.php`
- `app/Models/User.php` (supprimé)
- `app/Http/Controllers/{Accountability,Finance,Identity,Work}/.gitkeep`
- `app/Models/{Accountability,Finance,Identity,Platform,Work}/.gitkeep`
- `app/Policies/{Accountability,Finance,Identity,Platform,Work}/.gitkeep`
- `app/Services/{Accountability,Finance,Identity,Platform,Work}/.gitkeep`
- `bootstrap/app.php`
- `composer.json`
- `composer.lock`
- `config/app.php`
- `config/auth.php`
- `database/factories/UserFactory.php` (supprimé)
- `database/migrations/0001_01_01_000000_create_users_table.php` (supprimé)
- `database/migrations/0001_01_01_000001_create_cache_table.php` (supprimé)
- `database/seeders/DatabaseSeeder.php`
- `package.json`
- `package-lock.json`
- `phpstan.neon`
- `resources/css/app.css`
- `resources/js/app.js`
- `resources/js/Pages/Platform/Demo.vue`
- `resources/js/Pages/{Accountability,Finance,Identity,Work}/.gitkeep`
- `resources/views/app.blade.php`
- `routes/web.php`
- `tests/Feature/ExampleTest.php` (supprimé)
- `tests/Feature/FoundationTest.php`
- `tests/Feature/Http/HealthEndpointTest.php`
- `tests/Feature/MigrationFoundationTest.php`
- `tests/Feature/RepositoryHygieneTest.php`
- `tests/TestCase.php`
- `tests/Unit/ExampleTest.php` (supprimé)
- `vite.config.js`

## QA Results

### Review Date: 2026-07-18

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implémentation de bonne tenue. Les 10 critères sont couverts par **12 tests, 112 assertions**, tous
verts, avec Pint propre et Larastan niveau 6 sans erreur sur `app`, `bootstrap`, `config` et `routes`.

Le point remarquable est la **traçabilité** : chaque test est nommé d'après le critère qu'il valide
(`test_ac_4_health_endpoint_is_public_complete_and_sanitized`). Le lien exigence → test se lit sans
effort. C'est le motif que les 81 stories suivantes doivent reprendre.

`HealthController` est correctement conçu : état individuel par composant, `503` réservé à
l'indisponibilité de la base, dégradation du cache sans échec global. Le
`shouldRenderJsonWhen($request->is('up'))` de `bootstrap/app.php` est une attention juste — sans lui,
une erreur sur `/up` renverrait du HTML à une sonde qui attend du JSON.

**Vérification par injection.** J'ai testé la solidité de l'assertion de non-fuite plutôt que de la
supposer : en faisant fuiter volontairement le message d'exception dans la réponse, le test
`test_ac_5_database_failure_returns_an_explicit_http_failure` **échoue correctement**. L'assertion
protège réellement. Contrôleur restauré et suite revérifiée (12/12) après l'expérience.

### Refactoring Performed

Aucun. Les réserves identifiées relèvent de décisions de périmètre (suppression du modèle `User` et
de la référence `config/auth.php`) qui engagent la story 2.1 — elles reviennent au dev et au PO, pas
au relecteur.

### Compliance Check

- Coding Standards : ✓ — Form Requests sans objet ici, types de retour explicites, `env()` confiné à `config/`, PHPUnit et non Pest
- Project Structure : ✓ — cinq modules créés sous les cinq racines, aucun dossier racine nouveau
- Testing Strategy : ✓ — `tests/Feature/` et `tests/Feature/Http/`, un test par critère
- All ACs Met : ✓ — les 10 sont satisfaits ; AC7 est constaté factuellement plutôt qu'automatisé, ce que la story admet en le formulant comme une confirmation

### Improvements Checklist

- [ ] **Supprimer `app/Models/User.php` et `database/factories/UserFactory.php`, neutraliser la référence de `config/auth.php`** — ils décrivent un schéma `name`/`email`/`password` dont la migration est supprimée et qui n'existera jamais. La table `users` de la story 2.1 porte `people_id`, un téléphone normalisé, un état de cycle de vie et une colonne générée d'unicité conditionnelle. Un développeur qui trouve ce modèle l'**étendra** au lieu de construire le modèle personne / compte. *(ARCH-001)*
- [ ] **Exécuter `php artisan migrate:fresh` en local** — la base de développement contient encore `users`, `password_reset_tokens`, `sessions`, `cache` et `cache_locks`. Les migrations sont supprimées, les tables persistent : un clone frais du dépôt se comporterait différemment de cette machine. *(ENV-001)*
- [ ] **Renseigner le Dev Agent Record et cocher les 40 tâches** avant de passer la story à `Done`. *(PROC-001)*
- [ ] Remplacer `assertMatchesRegularExpression('/\+01:00"}$/')` par une assertion sur le champ `timestamp` décodé — la forme actuelle couple le test à l'ordre des champs JSON. *(TEST-001)*

### Security Review

**PASS.** `/up` n'expose ni secret, ni chemin de base, ni nom d'hôte interne, et la protection est
prouvée par injection (voir ci-dessus). Aucune surface d'authentification n'existe encore à ce stade.

### Performance Considerations

`app.js` pèse **245 kB brut / 84 kB gzip** pour Vue + Inertia et une page de démonstration vide,
`app.css` 55,7 kB / 11,8 kB gzip. NFR2 plafonne à 300 Ko au premier chargement : la marge est
confortable, mais c'est le **point de départ** avant les composants transverses de la story 1.7.
À surveiller, pas à corriger ici.

### Files Modified During Review

Aucun fichier de production modifié. `HealthController.php` a été temporairement altéré pour la
vérification par injection, puis restauré à l'identique (`diff` vérifié).

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/1.1-fondation-applicative-point-sante.yml`

Aucune réserve ne porte sur le code livré, qui satisfait les 10 critères. Les deux réserves
significatives concernent des **artefacts résiduels** qui induiront la story 2.1 en erreur, et un
**dossier de story vide** qui rompt la chaîne d'apprentissage vers la story 1.2.

### Recommended Status

**✗ Changes Required** — ARCH-001 et PROC-001 avant de passer à `Done`. Ce sont deux corrections
courtes ; le travail de fond est fait. *(Le propriétaire de la story décide du statut final.)*
