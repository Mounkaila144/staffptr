# Story 4.1: Calendrier des jours travaillés et jours fériés

## Status

Done

## Story

**En tant que** direction,
**je veux** déclarer les jours travaillés et les jours fériés,
**afin que** l'application n'attende pas de rapport un jour où l'entreprise est fermée.

## Acceptance Criteria

1. Les jours travaillés de la semaine sont **paramétrables**, initialisés du lundi au vendredi.
2. Des jours fériés ou de fermeture ponctuels sont saisissables avec libellé et date ; `HolidaySeeder` initialise les jours fériés nigériens de l'année en cours.
3. Une fonction applicative répond « jour travaillé : oui / non » pour toute date ; testée sur un jour ouvré, un samedi, un dimanche et un jour férié saisi.
4. Aucun rapport quotidien n'est attendu ni compté comme manquant sur un jour non travaillé (contrat consommé par Epic 6).
5. Toute modification du calendrier est auditée et porte une date d'effet.
6. La vue calendrier reste lisible à 320 px ; les jours non travaillés sont distingués **par un libellé** autant que par la couleur.

## Tasks / Subtasks

- [x] **Task 1 — Table `holidays` et modèle (module Platform)** (AC : 2, 5)
  - [x] `php artisan make:migration create_holidays_table`. Colonnes : `id`, `label` (string), `date` (`DATE`, calendrier civil `Africa/Niamey`, jamais converti), `is_active` (bool, défaut `true`), `timestamps(precision: 3)`. Unicité utile `(date)` ou `(date, label)` selon besoin. [Source: architecture/17-fuseau-horaire-et-devise.md, architecture/6-modle-de-donnes.md#61-rgles-transverses]
  - [x] ⛔ **`GRANT UPDATE` par table** : `holidays` est modifiée (renommage, (dés)activation) → accorder `UPDATE` dans la migration ; consigner dans `docs/ops/database-users.md`. Pas de `DELETE`. [Source: docs/ops/database-users.md, database/migrations/2026_07_19_230525_create_people_table.php]
  - [x] ⛔ **Aucune suppression physique** : un jour férié saisi par erreur est **désactivé** (`is_active = false`), jamais supprimé — même patron que `departments` (3.1). [Source: prd/socle-transverse.md#SOC-03, app/Models/Identity/Department.php]
  - [x] `php artisan make:model Platform/Holiday` : traits `Auditable` + `PreventsPhysicalDeletion`, `$fillable` explicite, casts (`date` → `immutable_date`, `is_active` → `boolean`), factory. [Source: app/Models/Platform/Attachment.php, app/Models/Identity/Person.php]

- [x] **Task 2 — `HolidaySeeder` idempotent, exécuté en production** (AC : 2)
  - [x] `php artisan make:seeder HolidaySeeder` : `updateOrCreate` des jours fériés **nigériens de l'année en cours**, rejouable sans effet de bord ; enregistré dans `DatabaseSeeder`. Donnée de référence exécutée en production. [Source: architecture/20-migrations-et-donnes-initiales.md#203-donnes-de-rfrence-excutes-en-production, database/seeders/DatabaseSeeder.php]
  - [x] ⛔ **Fériés à date mobile** (fêtes musulmanes : Aïd, Mawlid…) : leurs dates varient chaque année ; le seeder pose l'année **en cours** et la direction saisit les suivantes. Ne pas coder un calcul astronomique. À confirmer PO la liste exacte. [Source: prd.md (FR35)]

- [x] **Task 3 — Jours travaillés : consommer le paramètre déjà livré (3.4)** (AC : 1)
  - [x] ⛔ **`working_days` est déjà un paramètre** livré en 3.4 (`SettingsService::workingDays()`, seedé **lun-ven**). L'AC 1 est donc porté par le mécanisme de paramètres ; 4.1 **le consomme**, ne le redéfinit pas. L'édition des jours travaillés reste l'écran Paramètres (3.4) ; le calendrier l'**affiche**. [Source: app/Services/Platform/SettingsService.php#workingDays, stories/3.4.story.md#Completion Notes List]

- [x] **Task 4 — `CalendarService` : la fonction « jour travaillé oui/non »** (AC : 3, 4)
  - [x] `app/Services/Platform/CalendarService.php` avec `isWorkingDay(CarbonImmutable|string $date): bool` : `true` si le jour de la semaine ∈ `working_days` **et** aucun jour férié **actif** ne tombe ce jour-là. Dates raisonnées en `Africa/Niamey`. [Source: app/Services/Platform/SettingsService.php, architecture/17-fuseau-horaire-et-devise.md]
  - [x] ⛔ **AC 3 testé sur quatre cas** : un jour ouvré (oui), un **samedi** (non), un **dimanche** (non), un **jour férié saisi** un jour ouvré (non).
  - [x] ⛔ **AC 4 — contrat pour l'Epic 6** : exposer aussi les jours **attendus** / **non travaillés** d'une période (ex. `expectedWorkingDays(from, to)`), testé **directement** ici. « Aucun rapport attendu ni manquant sur un jour non travaillé » est **consommé par 6.x** — 4.1 livre et teste le contrat, pas l'usage rapport. [Source: prd/epic-4-calendrier-absences-depenses.md#story-41, prd/epic-6-rapport-quotidien-blocages.md]

- [x] **Task 5 — Autorisation, routes, audit** (AC : 2, 5)
  - [x] ⛔ **Permission `calendrier.gerer`** (à ajouter au catalogue), **`direction` seule** — cohérent E1.18 (« Calendrier — `direction` ») et FR35 (« saisissables par `direction` »). ⛔ **À valider PO** : `super_admin` n'y accède pas (contrairement à `parametre.gerer` qui l'inclut) ; d'où une permission dédiée plutôt que la réutilisation de `parametre.gerer`. [Source: docs/front-end-spec.md (E1.18), prd.md (FR35), config/permission-catalog.php]
  - [x] Routes (module Platform, `routes/web.php`) :
    - `calendar.index` — GET `/calendrier`
    - `holidays.store` — POST `/calendrier/jours-feries`
    - `holidays.update` — PATCH `/calendrier/jours-feries/{holiday}`
    - `holidays.deactivate` — PATCH `/calendrier/jours-feries/{holiday}/desactiver` (et `reactivate` symétrique)
  - [x] Déclarer **toutes** ces routes dans `config/authorization-matrix.php` (`calendrier.gerer`, `direction`) ; une route protégée non déclarée fait échouer la CI. [Source: config/authorization-matrix.php, prd/socle-transverse.md#SOC-01]
  - [x] `HolidayPolicy` (direction) enregistrée dans `AppServiceProvider::boot`. Écritures via service **transactionnel + audit** (SOC-02), actions `holiday_created`, `holiday_updated`, `holiday_deactivated`/`reactivated`. ⛔ **AC 5 — date d'effet** : les jours fériés portent leur `date` ; les modifications de `working_days` portent déjà `effective_at` (mécanisme 3.4). Enrichir `AuditLogService` (`Holiday`, actions, champs). [Source: app/Support/Auditing/AuditLogger.php, app/Services/Platform/AuditLogService.php, prd/socle-transverse.md#SOC-02]

- [x] **Task 6 — Vue calendrier lisible à 320 px** (AC : 6)
  - [x] Contrôleur `Http/Controllers/Platform/CalendarController.php` : `index` (Inertia render : jours travaillés courants + jours fériés du mois/de l'année), + actions holidays via Form Request. Validation exclusivement Form Request. [Source: architecture/source-tree.md#où-placer-quoi, prd/socle-transverse.md#SOC-11]
  - [x] Page `resources/js/Pages/Platform/Calendar/Index.vue` : vue calendrier + liste des jours fériés (ajout/désactivation). ⛔ **AC 6** : jours non travaillés distingués **par un libellé** (« Fermé », « Férié : … », « Week-end ») **autant que par la couleur** — jamais la couleur seule. Utilisable à 320 px sans défilement horizontal, cibles ≥ 44 × 44 px. [Source: prd/socle-transverse.md#SOC-09, prd/socle-transverse.md#SOC-10]
  - [x] État vide de la liste des fériés : « Aucun jour férié saisi pour cette période. » avec l'action d'ajout. [Source: prd/socle-transverse.md#SOC-06]

- [x] **Task 7 — Tests, un par critère d'acceptation** (AC : 1–6)
  - [x] HTTP/autorisation dans `tests/Feature/Http/` ; `CalendarService`, seeder, audit dans `tests/Feature/` ; logique pure de `isWorkingDay` dans `tests/Unit/` si extraite. Factories/states. [Source: architecture/source-tree.md#où-placer-quoi]
  - [x] **AC 1** : `working_days` paramétrable (déjà 3.4) consommé par le calendrier ; défaut lun-ven vérifié.
  - [x] **AC 2** : `HolidaySeeder` idempotent ; saisie d'un férié avec libellé et date.
  - [x] ⛔ **AC 3** : `isWorkingDay` sur jour ouvré / samedi / dimanche / férié saisi.
  - [x] **AC 4** : le contrat période (jours attendus / non travaillés) testé directement.
  - [x] **AC 5** : modification du calendrier auditée ; `direction` seule modifie, autres rôles (dont `super_admin`) `403` par URL directe.
  - [x] **AC 6** : rendu 320 px, libellé distinct de la couleur (test/revue).
  - [x] `php artisan test`, `vendor/bin/pint --dirty`, Larastan niveau 6, `npm run build` + budget ; suite verte **sur MySQL** en CI. [Source: prd/socle-transverse.md#SOC-05]

## Dev Notes

### Previous Story Insights

- 3.4 a livré `working_days` comme **paramètre éditable** (`SettingsService::workingDays()`, seedé lun-ven, audité avec `effective_at`). L'AC 1 de 4.1 est donc **déjà satisfait par le mécanisme** ; 4.1 consomme et affiche, il ne réimplémente pas les jours travaillés. C'est le premier consommateur de la famille « jours fériés/fermetures » annoncée en 3.4 comme livrée avec son consommateur. [Source: app/Services/Platform/SettingsService.php, stories/3.4.story.md#Completion Notes List]
- 3.1 a établi le patron « objet désactivable, jamais supprimé » (`departments.is_active`) que `holidays` reprend. [Source: app/Models/Identity/Department.php, stories/3.1.story.md]
- Le patron d'écriture auditée transactionnelle + enrichissement d'`AuditLogService` est stable depuis 3.1. [Source: app/Services/Platform/AuditLogService.php]

### Le calendrier = jours travaillés (paramètre 3.4) + jours fériés (table 4.1)

`CalendarService::isWorkingDay(date)` combine deux sources : le jour de la semaine ∈
`working_days` (paramètre 3.4) **et** l'absence de jour férié **actif** ce jour-là (table
`holidays`). Toutes les dates sont raisonnées en calendrier civil **`Africa/Niamey`**, jamais
converties. [Source: architecture/17-fuseau-horaire-et-devise.md, app/Services/Platform/SettingsService.php]

### Contrat pour l'Epic 6 (AC 4)

4.1 **livre et teste** le contrat « jour travaillé oui/non » et « jours attendus d'une période » ;
l'usage « aucun rapport attendu ni manquant sur un jour non travaillé » est **consommé par l'Epic 6**
(rapport quotidien). Même logique de frontière que 3.2 (`ContractEndingService` testé directement
sans son consommateur). Ne pas construire ici la logique de rapport. [Source: prd/epic-4-calendrier-absences-depenses.md#story-41, prd/epic-6-rapport-quotidien-blocages.md]

### Jours fériés — dates mobiles (AC 2)

`HolidaySeeder` pose les fériés nigériens de **l'année en cours** (fériés civils fixes + fêtes
musulmanes de l'année, à **date mobile**). ⛔ Ne pas coder un calcul astronomique des fêtes mobiles :
le seeder pose l'année courante, la direction saisit les suivantes. ⛔ **À confirmer PO** : la liste
exacte des jours fériés officiels du Niger. [Source: prd.md (FR35), architecture/20-migrations-et-donnes-initiales.md#203-donnes-de-rfrence-excutes-en-production]

### Autorisation (AC 5) — `direction` seule

E1.18 (« Calendrier — `direction` ») et FR35 (« saisissables par `direction` ») donnent le calendrier
à `direction` **seule**. ⛔ Une permission dédiée **`calendrier.gerer`** (direction) est proposée
plutôt que `parametre.gerer` (qui inclut `super_admin`) : le calendrier est une donnée métier, pas un
réglage technique. `super_admin` reçoit `403`. ⛔ **À valider PO**. Note : l'édition des **jours
travaillés** reste l'écran Paramètres (3.4, `parametre.gerer`) — le calendrier les affiche seulement.
[Source: docs/front-end-spec.md (E1.18), prd.md (FR35), config/permission-catalog.php]

### Date d'effet (AC 5)

Les modifications de `working_days` portent déjà une **date d'effet** via le mécanisme 3.4
(`effective_at`, audité). Les jours fériés portent leur propre `date` ; leur création/désactivation
est auditée avec ancienne/nouvelle valeur. [Source: stories/3.4.story.md, app/Support/Auditing/AuditLogger.php]

### Placement (module Platform)

- Modèle → `app/Models/Platform/Holiday.php` ; migration → `…_create_holidays_table.php` ; seeder → `database/seeders/HolidaySeeder.php`
- Service → `app/Services/Platform/CalendarService.php` ; contrôleur → `app/Http/Controllers/Platform/CalendarController.php` ; Form Requests → `app/Http/Requests/Platform/`
- Policy → `app/Policies/Platform/HolidayPolicy.php` (enregistrée dans `AppServiceProvider::boot`)
- Écran → `resources/js/Pages/Platform/Calendar/Index.vue` ; routes → `routes/web.php` + matrice.
[Source: architecture/source-tree.md#les-cinq-modules (Platform → Holiday), architecture/source-tree.md#où-placer-quoi]

### Testing

- **PHPUnit.** Autorisation/routes dans `tests/Feature/Http/` ; `CalendarService`, seeder, audit dans `tests/Feature/` ; `isWorkingDay` pur dans `tests/Unit/`. Factories/states. [Source: architecture/testing-strategy.md, architecture/source-tree.md#où-placer-quoi]
- **Un test Feature traçable par critère d'acceptation**, nommé d'après lui. [Source: prd/socle-transverse.md#SOC-05]
- ⛔ **AC 3** : quatre cas (ouvré / samedi / dimanche / férié saisi).
- ⛔ **AC 4** : contrat période testé directement (sans logique de rapport).
- **AC 5** : `direction` seule modifie ; `super_admin` et rôles métier `403` ; modifications auditées.
- Suite verte **sur MySQL** en CI ; `pint --dirty`, Larastan niveau 6, build et budget verts. [Source: prd/socle-transverse.md#SOC-05]

### Project Structure Notes

- **Une table métier nouvelle mutable** (`holidays`) → `GRANT UPDATE` (migration + `database-users.md`), pas de `DELETE` ; désactivation plutôt que suppression. [Source: docs/ops/database-users.md]
- **AC 1 déjà livré par 3.4** : ne pas dupliquer `working_days` ; le consommer.
- **Deux décisions PO** : (a) permission dédiée `calendrier.gerer` (direction) vs `parametre.gerer` ; (b) liste officielle des jours fériés nigériens (dates mobiles).
- **Contrat pour l'Epic 6** : `CalendarService` doit rester la **seule** source de « jour travaillé oui/non » — les indicateurs de ponctualité (6.x) ne réimplémentent jamais ce calcul.
- **Nouvel epic** : première story de l'Epic 4. Dépend d'Epic 3 (paramètres) et Epic 2 (comptes direction). Ni compte financier ni paiement dans cet epic (ÉCART-01).
- **Dépendance de l'écran 2.10** : enrichir `AuditLogService` fait partie du terminé.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-21 | 1.0 | Création du brouillon de la story 4.1 (première de l'Epic 4) : jours fériés en table `holidays` désactivable/auditée + `HolidaySeeder` idempotent, consommation du paramètre `working_days` déjà livré en 3.4, `CalendarService::isWorkingDay` testé sur quatre cas et contrat de période pour l'Epic 6, vue calendrier lisible à 320 px avec libellé distinct de la couleur, permission dédiée `calendrier.gerer` (direction) ; liste des fériés et choix de permission signalés pour le PO | Bob (Scrum Master) |
| 2026-07-21 | 1.1 | Implémentation complète : domaine et seeder des jours fériés, service calendrier et contrat Epic 6, autorisation direction seule, audit transactionnel, écran responsive de gestion et tests AC 1–6 | James (Developer) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `/Applications/MAMP/bin/php/php8.3.30/bin/php artisan test tests/Feature/CalendarServiceTest.php tests/Feature/HolidaySeederTest.php tests/Feature/Http/CalendarHttpTest.php`
- `/Applications/MAMP/bin/php/php8.3.30/bin/php artisan test tests/Feature/Http/AuthorizationMatrixTest.php tests/Feature/RbacFoundationTest.php tests/Feature/CalendarServiceTest.php tests/Feature/HolidaySeederTest.php tests/Feature/Http/CalendarHttpTest.php`
- `/Applications/MAMP/bin/php/php8.3.30/bin/php artisan test` — 354 tests, 8 306 assertions, 26 tests ignorés explicitement
- `/Applications/MAMP/bin/php/php8.3.30/bin/php vendor/bin/pint --dirty`
- `/Applications/MAMP/bin/php/php8.3.30/bin/php vendor/bin/phpstan analyse --no-progress`
- `npm run test:ci-scripts`, `npm run build`, `npm run check:bundle`

### Completion Notes List

- Table `holidays` à date civile `Africa/Niamey`, unicité par date, désactivation sans suppression, privilège MySQL `UPDATE` explicite et audit dans la transaction métier.
- `HolidaySeeder` idempotent enregistré en production : 16 fêtes légales 2026, dont Lailatoul Qadr, les deux jours de l’Aïd el-Fitr, la Refondation, les deux jours de Tabaski, le Nouvel An musulman et le 26 juillet. Les dates mobiles futures restent saisies par la direction.
- Décision PO confirmée pendant le développement : permission dédiée `calendrier.gerer` pour `direction` uniquement ; `super_admin` et les rôles métier reçoivent `403`.
- `CalendarService` consomme `working_days`, centralise `isWorkingDay`, `expectedWorkingDays`, `nonWorkingDays` et les libellés « Fermé », « Week-end » et « Férié : … ».
- Écran Inertia/Vue utilisable à 320 px avec navigation mensuelle, création, modification, désactivation/réactivation et état vide actionnable.
- Régression locale verte sur SQLite ; la suite CI reste configurée pour MySQL 8 afin de valider les garanties spécifiques au moteur.
- Aucune dépendance ni variable d’environnement ajoutée.

### File List

- `app/Http/Controllers/Platform/CalendarController.php`
- `app/Http/Requests/Platform/CalendarIndexRequest.php`
- `app/Http/Requests/Platform/ChangeHolidayActivityRequest.php`
- `app/Http/Requests/Platform/StoreHolidayRequest.php`
- `app/Http/Requests/Platform/UpdateHolidayRequest.php`
- `app/Models/Platform/Holiday.php`
- `app/Policies/Platform/HolidayPolicy.php`
- `app/Providers/AppServiceProvider.php`
- `app/Services/Platform/AuditLogService.php`
- `app/Services/Platform/CalendarService.php`
- `app/Services/Platform/HolidayService.php`
- `config/authorization-matrix.php`
- `config/permission-catalog.php`
- `database/factories/Platform/HolidayFactory.php`
- `database/migrations/2026_07_21_140702_create_holidays_table.php`
- `database/seeders/DatabaseSeeder.php`
- `database/seeders/HolidaySeeder.php`
- `docs/ops/database-users.md`
- `package.json`
- `resources/js/Layouts/AppLayout.vue`
- `resources/js/Pages/Platform/Calendar/Index.vue`
- `routes/web.php`
- `tests/Feature/CalendarServiceTest.php`
- `tests/Feature/HolidaySeederTest.php`
- `tests/Feature/Http/CalendarHttpTest.php`
- `tests/js/calendar-page.test.mjs`

## QA Results
